# Day 37 — Repo Metadata Package

## 04. GitHub Folder Structure
```
Day_37_SQL_Filtering_Sorting_Nulls/
│
├── notebook.ipynb        (executed — hands-on practice + outputs)
├── notes.md               (includes Quick Overview at top)
├── dataset.db              (SQLite database used for all queries)
└── images/
    ├── day_37_where_equals.png
    ├── day_37_and_operator.png
    ├── day_37_order_by_desc.png
    ├── day_37_or_operator.png
    ├── day_37_distinct.png
    └── day_37_is_null.png
```
*Note: this topic's source data is a SQLite database rather than a CSV, so `dataset.db` replaces `dataset.csv` in the standard layout.*

## 05. Commit Message
```
Day 37: Learned SQL WHERE filtering, AND/OR logic, ORDER BY, DISTINCT, and NULL handling
```

## 06. Repository Description
```
Day 37/90 — SQL filtering, AND/OR logic, sorting, and NULL handling, beginner-friendly.
```
*(89 characters — under the 120-char limit)*

## 07. File Names
- `notebook.ipynb`
- `notes.md`
- `dataset.db`
- `day_37_where_equals.png`
- `day_37_and_operator.png`
- `day_37_order_by_desc.png`
- `day_37_or_operator.png`
- `day_37_distinct.png`
- `day_37_is_null.png`

## 08. SEO Keywords (GitHub topics)
```
sql
sqlite
data-science
where-clause
data-filtering
learning-journey
90-day-challenge
jupyter-notebook
beginner-friendly
db-browser-for-sqlite
```

## 09. Optional Visuals
No extra charts needed for this topic — it's pure query logic, not numeric analysis. The 6 DB Browser screenshots below (deliverable 10) already carry the visual weight for this day.

## 10. Code Screenshots (DB Browser for SQLite style — 6 total)
Style chosen: **DB Browser for SQLite** spec — Day 37 is a pure-SQL day (queries run directly against `day_37.db` in DB Browser, no Python/pandas involved), so the SQLite visual spec applies rather than VS Code.

One shot per distinct taught concept, no padding (exactly 6 concepts taught → 6 shots):
1. `day_37_where_equals.png` — exact-match `WHERE`
2. `day_37_and_operator.png` — combining conditions with `AND`
3. `day_37_order_by_desc.png` — sorting with `ORDER BY ... DESC`
4. `day_37_or_operator.png` — combining conditions with `OR`
5. `day_37_distinct.png` — unique values with `DISTINCT`
6. `day_37_is_null.png` — missing-data check with `IS NULL`

Rendered at 1200×900 using the locked DB Browser palette (panel `#eceff1`, keyword/status `#0277bd`, string `#c2185b`, comment `#78909c`, titlebar `#37474f`). These attach to the LinkedIn post (deliverable 01) as images — the post text isn't rewritten to describe them.
